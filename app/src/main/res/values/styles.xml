<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="RoundedImageViewStyle">
        <item name="android:background">@drawable/rounded_image</item>
    </style>
    <!-- Define the custom shape appearance -->
    <style name="CustomShapeAppearance">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSizeTopLeft">18dp</item>
        <item name="cornerSizeTopRight">18dp</item>
        <item name="cornerSizeBottomLeft">0dp</item>
        <item name="cornerSizeBottomRight">0dp</item>
    </style>

    <!-- Define the custom shape appearance -->
    <style name="CustomShapeAppearance2">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSizeTopLeft">20dp</item>
        <item name="cornerSizeTopRight">0dp</item>
        <item name="cornerSizeBottomLeft">20dp</item>
        <item name="cornerSizeBottomRight">0dp</item>
    </style>

    <!-- Define the style with a black outline -->
    <style name="ShapeWithOutline">
        <item name="android:background">@drawable/rounded_image</item>
        <item name="shapeAppearanceOverlay">@style/CustomShapeAppearance</item>
    </style>

    <style name="ShapeWithOutline2">
        <item name="android:background">@drawable/rounded_image2</item>
        <item name="shapeAppearanceOverlay">@style/CustomShapeAppearance2</item>
    </style>

    <style name="Theme.TEST01" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryDark">@color/purple_700</item>
        <item name="colorAccent">@color/teal_200</item>
    </style>

    <!-- NoActionBar variant of the theme -->
    <style name="Theme.TEST01.NoActionBar" parent="Theme.TEST01">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/Orange1</item>
        <item name="colorPrimaryDark">@color/Orange2</item>
        <item name="colorAccent">@color/Orange3</item>
    </style>

    <style name="CustomEditText">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginTop">12dp</item>
        <item name="android:layout_marginBottom">12dp</item>
        <item name="android:background">@color/white</item>
        <item name="android:padding">12dp</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textSize">16sp</item>
    </style>


</resources>
